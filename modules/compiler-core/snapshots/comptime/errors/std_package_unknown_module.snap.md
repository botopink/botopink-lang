----- SOURCE CODE
import {linked_list} from "std";

----- ERROR
error: unknown "std" module in import
  ┌─ :1:9
  │
1 │ import {linked_list} from "std";
  │         ^

  hint: Available std modules: bool. (`result` is builtin — call `result.map(r, f)` without importing.)
