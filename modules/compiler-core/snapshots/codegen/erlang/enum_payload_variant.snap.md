----- SOURCE CODE -- main.bp
```botopink
val Color = type {
    Red,
    Rgb(r: i32, g: i32, b: i32),
}
```

----- ERLANG -- main.erl
```erlang
-module(main).

%% type Color
%%   Red
%%   Rgb(r, g, b)
```

----- RUN LOG -----
```logs
```
