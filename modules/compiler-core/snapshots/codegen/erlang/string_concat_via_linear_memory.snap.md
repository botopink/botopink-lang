----- SOURCE CODE -- main.bp
```botopink
fn greeting() -> string {
    return "Hello, " + "World";
}
```

----- ERLANG -- main.erl
```erlang
-module(test@main).

greeting() ->
    <<"Hello, ", "World">>.
```

----- RUN LOG -----
```logs
```
