----- SOURCE CODE -- main.bp
```botopink
fn main() {
    // Initialize value
    val x = 1;
    // Return null
    null;
}
```

----- ERLANG -- main.erl
```erlang
-module(main).

main() ->
    % Initialize value,
    X = 1,
    % Return null,
    undefined.
```
