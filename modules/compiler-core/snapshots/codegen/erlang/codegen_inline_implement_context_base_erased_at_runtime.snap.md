----- SOURCE CODE -- main.bp
```botopink
val Element = type implement @Context<Element, Element> { }
fn render() -> Element {
    Element();
}
```

----- ERLANG -- main.erl
```erlang
-module(main).

%% type Element: 

render() ->
    #{}.
```

----- RUN LOG -----
```logs
```
