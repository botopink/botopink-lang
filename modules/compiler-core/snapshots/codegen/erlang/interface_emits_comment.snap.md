----- SOURCE CODE -- main.bp
```botopink
val Drawable = behavior {
    val color: string;
    fn draw(self: Self);
}
```

----- ERLANG -- main.erl
```erlang
-module(main).

%% behavior Drawable
```

----- RUN LOG -----
```logs
```
