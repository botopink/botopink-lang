----- SOURCE CODE -- main.bp
```botopink
val Drawable = behavior {
    val color: string;
    fn draw(self: Self);
}
```

----- BEAM ASSEMBLY -- main.S
```erlang
{module, main}.
{exports, []}.
{attributes, []}.
{labels, 2}.
```

----- RUN LOG -----
```logs
```
