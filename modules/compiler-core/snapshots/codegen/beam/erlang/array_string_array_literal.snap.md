----- SOURCE CODE -- main.bp
```botopink
val xs = ["hello", "world"];
```

----- ERLANG -- main.erl
```erlang
-module(test@main).

xs() ->
    [<<"hello">>, <<"world">>].
```

----- RUN LOG -----
```logs
```
