----- SOURCE CODE -- main.bp
```botopink
/// This function greets the user
fn greet(name: string) -> string {
    return name;
}
```

----- ERLANG -- main.erl
```erlang
-module(test@main).

%% This function greets the user

greet(Name) ->
    Name.
```

----- RUN LOG -----
```logs
```
