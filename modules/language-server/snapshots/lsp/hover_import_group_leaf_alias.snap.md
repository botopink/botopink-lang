----- SOURCE
```botopink
import {collections: {gt, reverse as flip}} from "std";
val d = flip(gt());
        ↑
```

----- HOVER at (line 1, char 8)
kind: markdown

```botopink
flip : fn(Order) -> Order
```
