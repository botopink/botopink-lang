----- SOURCE
```botopink
fn counter() -> @Iterator<i32> :gen { yield 1; }
   ↑
```

----- HOVER at (line 0, char 3)
kind: markdown

```botopink
fn counter() -> @Iterator<i32> :gen
```

---

`for` item type: `i32`
