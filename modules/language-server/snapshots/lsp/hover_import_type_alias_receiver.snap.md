----- SOURCE
```botopink
import {collections.Dict as D} from "std";
val d: D<string, i32> = D.empty();
                        ↑
```

----- HOVER at (line 1, char 24)
kind: markdown

```botopink
D = Dict
```
