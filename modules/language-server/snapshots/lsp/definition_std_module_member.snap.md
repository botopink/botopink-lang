----- SOURCE
```botopink
import {collections} from "std";
val n = collections.toInt(collections.lt());
                    ↑
```

----- DEFINITION at (line 1, char 20)
uri: std/collections
range: (512,7) → (512,12)
in std/collections:
