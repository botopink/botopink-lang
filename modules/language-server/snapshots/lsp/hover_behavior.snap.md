----- SOURCE
```botopink
pub behavior Mappable<T> {
             ↑
    fn map(self: Self<T>) -> Self<T>;
}
```

----- HOVER at (line 0, char 13)
kind: markdown

```botopink
pub behavior Mappable<T>
```
