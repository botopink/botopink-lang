----- SOURCE
```botopink
import {dict.empty as newDict} from "std";
val d = newDict();
        ↑
```

----- HOVER at (line 1, char 8)
kind: markdown

```botopink
newDict : fn() -> Dict<T122, T123>
```
