----- SOURCE
```botopink
fn load() -> #(name: string, pop: i32) { return #("SP", 12); }
val row = load();
    ↑
```

----- HOVER at (line 1, char 4)
kind: markdown

```botopink
val row : #(name: string, pop: i32)
```
