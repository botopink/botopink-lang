----- SOURCE
```botopink
val Element = type implement @Context<Element> { }
fn state(initial: i32) -> @Component<Element, i32> {
   ↑
    initial;
}
```

----- HOVER at (line 1, char 3)
kind: markdown

```botopink
fn state(initial: i32) -> @Component<Element, i32>
```

---

`await` value type: `i32`
