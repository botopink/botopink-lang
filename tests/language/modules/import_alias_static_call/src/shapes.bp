//// Types a consumer imports under other names.
pub type Point(x: i32, y: i32) {
    pub fn origin() -> Point { return Point(x: 0, y: 0); }
    pub fn sum(self: Self) -> i32 { return self.x + self.y; }
}

pub type Shade { Light, Dark, Custom(level: i32) }
