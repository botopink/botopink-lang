//// Pending 0203-a, answered (b): a name no primitive interface declares is
//// refused at the call — it used to type as a fresh variable and reach the
//// host, where `fooBar` does not exist either.

fn main() {
    @println("x".fooBar());
}
