//// Decision 8 §1.3 — explicit type arguments name the callee's type
//// parameters one for one; a count that does not match is refused at the call.

type Pair<A, B>(a: A, b: B)

pub fn main() {
    val p = Pair<i32>(a: 1, b: 2);
    @print(p.a);
}
