# Library cells `zig build test-libs` knows to be red, each owned by the front
# that turns it green. One cell per line:
#
#   <lib> <target> <owner> <reason…>
#
# A listed cell that fails is reported by name and counted, but does not fail
# the gate. A listed cell that PASSES fails the gate until its line is deleted —
# the owning front deletes it in the commit that fixes the library, which turns
# the cell into a hard assert. An unlisted cell that fails always fails the gate.
#
# Owners are the 1.0.4-beta fronts (specs/1.0.4-beta/fronts.md).


# ── decision 88 (1.0.10-beta, front 19 `use` activation) ────────────────────
# A body activates a hook only under `#[@context]` (use-without-context-effect).
# jhonstart's `src/hooks.bp:104-117` writes `fn Counter() -> Element { val c = use
# state(0); … }` without the annotation; the library front adds `#[@context]` to
# its components and custom hooks (and, per the same decision, provides
# `state`/`effect`/`memo` as cells on the client build). Owner: the 1.0.10-beta
# jhonstart library front (`specs/1.0.10-beta/04-jhonstart`, decision 88 sweep).
jhonstart commonJS 04-jhonstart hooks.bp:109 `use state(0)` in `Counter() -> Element` without `#[@context]` — use-without-context-effect (decision 88)
jhonstart erlang 04-jhonstart hooks.bp:109 `use state(0)` in `Counter() -> Element` without `#[@context]` — use-without-context-effect (decision 88)
