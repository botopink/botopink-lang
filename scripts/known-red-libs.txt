# Library cells `zig build test-libs` knows to be red, each owned by the front
# that turns it green. One cell per line:
#
#   <lib> <target> <owner> <reason…>
#
# A listed cell that fails is reported by name and counted, but does not fail
# the gate. A listed cell that PASSES fails the gate until its line is deleted —
# the owning front deletes it in the commit that fixes the library, which turns
# the cell into a hard assert. An unlisted cell that fails always fails the gate.
#
# Owners are the 1.0.4-beta fronts (specs/1.0.4-beta/fronts.md).
emilia-card commonJS 22-loops/4 decision 105 refuses `loop (…)` (front 22 step 1); the jhonstart this checkout reads still spells it — the sweep is jhonstart `front/22-loops` 4a8b41f, and the line leaves when that lands where the gate reads it
erika commonJS 22-loops/4 decision 105 refuses `loop (…)` (front 22 step 1); the erika this checkout reads still spells it — the sweep is erika `front/22-loops` 0f099f6, and the line leaves when that lands where the gate reads it
erika erlang 22-loops/4 decision 105 refuses `loop (…)` (front 22 step 1); the erika this checkout reads still spells it — the sweep is erika `front/22-loops` 0f099f6, and the line leaves when that lands where the gate reads it
erika-linq commonJS 22-loops/4 decision 105 refuses `loop (…)` (front 22 step 1); the erika this checkout reads still spells it — the sweep is erika `front/22-loops` 0f099f6, and the line leaves when that lands where the gate reads it
jhonstart commonJS 22-loops/4 decision 105 refuses `loop (…)` (front 22 step 1); the jhonstart this checkout reads still spells it — the sweep is jhonstart `front/22-loops` 4a8b41f, and the line leaves when that lands where the gate reads it
jhonstart erlang 22-loops/4 decision 105 refuses `loop (…)` (front 22 step 1); the jhonstart this checkout reads still spells it — the sweep is jhonstart `front/22-loops` 4a8b41f, and the line leaves when that lands where the gate reads it
jhonstart-counter commonJS 22-loops/4 decision 105 refuses `loop (…)` (front 22 step 1); the jhonstart this checkout reads still spells it — the sweep is jhonstart `front/22-loops` 4a8b41f, and the line leaves when that lands where the gate reads it
jhonstart-html commonJS 22-loops/4 decision 105 refuses `loop (…)` (front 22 step 1); the jhonstart this checkout reads still spells it — the sweep is jhonstart `front/22-loops` 4a8b41f, and the line leaves when that lands where the gate reads it
jhonstart-html erlang 22-loops/4 decision 105 refuses `loop (…)` (front 22 step 1); the jhonstart this checkout reads still spells it — the sweep is jhonstart `front/22-loops` 4a8b41f, and the line leaves when that lands where the gate reads it
jhonstart-todo commonJS 22-loops/4 decision 105 refuses `loop (…)` (front 22 step 1); the jhonstart this checkout reads still spells it — the sweep is jhonstart `front/22-loops` 4a8b41f, and the line leaves when that lands where the gate reads it
rakun commonJS 22-loops/4 decision 105 refuses `loop (…)` (front 22 step 1); the rakun this checkout reads still spells it — the sweep is rakun `front/22-loops` 1236eaa, and the line leaves when that lands where the gate reads it
rakun-actuator erlang 22-loops/4 decision 105 refuses `loop (…)` (front 22 step 1); the rakun this checkout reads still spells it — the sweep is rakun `front/22-loops` 1236eaa, and the line leaves when that lands where the gate reads it
rakun-cache erlang 22-loops/4 decision 105 refuses `loop (…)` (front 22 step 1); the rakun this checkout reads still spells it — the sweep is rakun `front/22-loops` 1236eaa, and the line leaves when that lands where the gate reads it
rakun-client erlang 22-loops/4 decision 105 refuses `loop (…)` (front 22 step 1); the rakun this checkout reads still spells it — the sweep is rakun `front/22-loops` 1236eaa, and the line leaves when that lands where the gate reads it
rakun-container-example commonJS 22-loops/4 decision 105 refuses `loop (…)` (front 22 step 1); the rakun this checkout reads still spells it — the sweep is rakun `front/22-loops` 1236eaa, and the line leaves when that lands where the gate reads it
rakun-data erlang 22-loops/4 decision 105 refuses `loop (…)` (front 22 step 1); the rakun this checkout reads still spells it — the sweep is rakun `front/22-loops` 1236eaa, and the line leaves when that lands where the gate reads it
rakun-example commonJS 22-loops/4 decision 105 refuses `loop (…)` (front 22 step 1); the rakun this checkout reads still spells it — the sweep is rakun `front/22-loops` 1236eaa, and the line leaves when that lands where the gate reads it
rakun-hateoas erlang 22-loops/4 decision 105 refuses `loop (…)` (front 22 step 1); the rakun this checkout reads still spells it — the sweep is rakun `front/22-loops` 1236eaa, and the line leaves when that lands where the gate reads it
rakun-logging erlang 22-loops/4 decision 105 refuses `loop (…)` (front 22 step 1); the rakun this checkout reads still spells it — the sweep is rakun `front/22-loops` 1236eaa, and the line leaves when that lands where the gate reads it
rakun-messaging erlang 22-loops/4 decision 105 refuses `loop (…)` (front 22 step 1); the rakun this checkout reads still spells it — the sweep is rakun `front/22-loops` 1236eaa, and the line leaves when that lands where the gate reads it
rakun-scheduling erlang 22-loops/4 decision 105 refuses `loop (…)` (front 22 step 1); the rakun this checkout reads still spells it — the sweep is rakun `front/22-loops` 1236eaa, and the line leaves when that lands where the gate reads it
rakun-security erlang 22-loops/4 decision 105 refuses `loop (…)` (front 22 step 1); the rakun this checkout reads still spells it — the sweep is rakun `front/22-loops` 1236eaa, and the line leaves when that lands where the gate reads it
rakun-session erlang 22-loops/4 decision 105 refuses `loop (…)` (front 22 step 1); the rakun this checkout reads still spells it — the sweep is rakun `front/22-loops` 1236eaa, and the line leaves when that lands where the gate reads it
rakun-ssr-example commonJS 22-loops/4 decision 105 refuses `loop (…)` (front 22 step 1); the rakun this checkout reads still spells it — the sweep is rakun `front/22-loops` 1236eaa, and the line leaves when that lands where the gate reads it
rakun-test commonJS 22-loops/4 decision 105 refuses `loop (…)` (front 22 step 1); the rakun this checkout reads still spells it — the sweep is rakun `front/22-loops` 1236eaa, and the line leaves when that lands where the gate reads it
rakun-test erlang 22-loops/4 decision 105 refuses `loop (…)` (front 22 step 1); the rakun this checkout reads still spells it — the sweep is rakun `front/22-loops` 1236eaa, and the line leaves when that lands where the gate reads it
rakun-validation commonJS 22-loops/4 decision 105 refuses `loop (…)` (front 22 step 1); the rakun this checkout reads still spells it — the sweep is rakun `front/22-loops` 1236eaa, and the line leaves when that lands where the gate reads it
rakun-validation erlang 22-loops/4 decision 105 refuses `loop (…)` (front 22 step 1); the rakun this checkout reads still spells it — the sweep is rakun `front/22-loops` 1236eaa, and the line leaves when that lands where the gate reads it
rakun-web erlang 22-loops/4 decision 105 refuses `loop (…)` (front 22 step 1); the rakun this checkout reads still spells it — the sweep is rakun `front/22-loops` 1236eaa, and the line leaves when that lands where the gate reads it
