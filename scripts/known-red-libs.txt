# Library cells `zig build test-libs` knows to be red, each owned by the front
# that turns it green. One cell per line:
#
#   <lib> <target> <owner> <reason…>
#
# A listed cell that fails is reported by name and counted, but does not fail
# the gate. A listed cell that PASSES fails the gate until its line is deleted —
# the owning front deletes it in the commit that fixes the library, which turns
# the cell into a hard assert. An unlisted cell that fails always fails the gate.
#
# Owners are the 1.0.4-beta fronts (specs/1.0.4-beta/fronts.md).
rakun                   commonJS  23-std-purity  decision 106's tree: std's flat modules moved to `collections` / `hash` / `encoding` and under `io/` / `testing/`, and the library still imports the flat names; deleted by the commit after rakun's std sweep (`front/23-std-purity`)
rakun-actuator          erlang    23-std-purity  decision 106's tree: std's flat modules moved to `collections` / `hash` / `encoding` and under `io/` / `testing/`, and the library still imports the flat names; deleted by the commit after rakun's std sweep (`front/23-std-purity`)
rakun-cache             erlang    23-std-purity  decision 106's tree: std's flat modules moved to `collections` / `hash` / `encoding` and under `io/` / `testing/`, and the library still imports the flat names; deleted by the commit after rakun's std sweep (`front/23-std-purity`)
rakun-client            erlang    23-std-purity  decision 106's tree: std's flat modules moved to `collections` / `hash` / `encoding` and under `io/` / `testing/`, and the library still imports the flat names; deleted by the commit after rakun's std sweep (`front/23-std-purity`)
rakun-container-example commonJS  23-std-purity  decision 106's tree: std's flat modules moved to `collections` / `hash` / `encoding` and under `io/` / `testing/`, and the library still imports the flat names; deleted by the commit after rakun's std sweep (`front/23-std-purity`)
rakun-data              erlang    23-std-purity  decision 106's tree: std's flat modules moved to `collections` / `hash` / `encoding` and under `io/` / `testing/`, and the library still imports the flat names; deleted by the commit after rakun's std sweep (`front/23-std-purity`)
rakun-example           commonJS  23-std-purity  decision 106's tree: std's flat modules moved to `collections` / `hash` / `encoding` and under `io/` / `testing/`, and the library still imports the flat names; deleted by the commit after rakun's std sweep (`front/23-std-purity`)
rakun-hateoas           erlang    23-std-purity  decision 106's tree: std's flat modules moved to `collections` / `hash` / `encoding` and under `io/` / `testing/`, and the library still imports the flat names; deleted by the commit after rakun's std sweep (`front/23-std-purity`)
rakun-logging           erlang    23-std-purity  decision 106's tree: std's flat modules moved to `collections` / `hash` / `encoding` and under `io/` / `testing/`, and the library still imports the flat names; deleted by the commit after rakun's std sweep (`front/23-std-purity`)
rakun-messaging         erlang    23-std-purity  decision 106's tree: std's flat modules moved to `collections` / `hash` / `encoding` and under `io/` / `testing/`, and the library still imports the flat names; deleted by the commit after rakun's std sweep (`front/23-std-purity`)
rakun-scheduling        erlang    23-std-purity  decision 106's tree: std's flat modules moved to `collections` / `hash` / `encoding` and under `io/` / `testing/`, and the library still imports the flat names; deleted by the commit after rakun's std sweep (`front/23-std-purity`)
rakun-security          erlang    23-std-purity  decision 106's tree: std's flat modules moved to `collections` / `hash` / `encoding` and under `io/` / `testing/`, and the library still imports the flat names; deleted by the commit after rakun's std sweep (`front/23-std-purity`)
rakun-session           erlang    23-std-purity  decision 106's tree: std's flat modules moved to `collections` / `hash` / `encoding` and under `io/` / `testing/`, and the library still imports the flat names; deleted by the commit after rakun's std sweep (`front/23-std-purity`)
rakun-ssr-example       commonJS  23-std-purity  decision 106's tree: std's flat modules moved to `collections` / `hash` / `encoding` and under `io/` / `testing/`, and the library still imports the flat names; deleted by the commit after rakun's std sweep (`front/23-std-purity`)
rakun-test              commonJS  23-std-purity  decision 106's tree: std's flat modules moved to `collections` / `hash` / `encoding` and under `io/` / `testing/`, and the library still imports the flat names; deleted by the commit after rakun's std sweep (`front/23-std-purity`)
rakun-test              erlang    23-std-purity  decision 106's tree: std's flat modules moved to `collections` / `hash` / `encoding` and under `io/` / `testing/`, and the library still imports the flat names; deleted by the commit after rakun's std sweep (`front/23-std-purity`)
rakun-web               erlang    23-std-purity  decision 106's tree: std's flat modules moved to `collections` / `hash` / `encoding` and under `io/` / `testing/`, and the library still imports the flat names; deleted by the commit after rakun's std sweep (`front/23-std-purity`)
