# Library cells `zig build test-libs` knows to be red, each owned by the front
# that turns it green. One cell per line:
#
#   <lib> <target> <owner> <reason…>
#
# A listed cell that fails is reported by name and counted, but does not fail
# the gate. A listed cell that PASSES fails the gate until its line is deleted —
# the owning front deletes it in the commit that fixes the library, which turns
# the cell into a hard assert. An unlisted cell that fails always fails the gate.
#
# Owners are the 1.0.4-beta fronts (specs/1.0.4-beta/fronts.md).
rakun commonJS 03-rakun `modules/rakun` declares `pub fn sub(string, i32, i32)` and `pub fn charOf(string, i32)` TWICE — byte-identical copies in `src/config.bp` and in `src/file_router.bp` — and `test/config_test.bp` imports both with the bare `import { … };` form, which names no module. Which copy it bound was the walk order; `00 · 13-module-identity` counts the declarations now and refuses instead of picking one (`CrossModule.pick` → `export_faults`), so the cell does not compile: "`sub` is declared `pub` by `file_router` and by `config`, and this import does not say which". Measured on `fix/cross-module-exports` with the refusal reverted in place: 302 pass / 0 fail before, `1 module(s) failed to compile: config_test` after. Two one-line fixes in rakun, either of which deletes this line: spell the import `from "config"`, or move the two helpers into one module and import them from it
