# Library cells `zig build test-libs` knows to be red, each owned by the front
# that turns it green. One cell per line:
#
#   <lib> <target> <owner> <reason…>
#
# A listed cell that fails is reported by name and counted, but does not fail
# the gate. A listed cell that PASSES fails the gate until its line is deleted —
# the owning front deletes it in the commit that fixes the library, which turns
# the cell into a hard assert. An unlisted cell that fails always fails the gate.
#
# Owners are the 1.0.4-beta fronts (specs/1.0.4-beta/fronts.md).

# `front/34-modifiers` in `repository/emilia` (landed 2026-09-21 05:01, emilia
# `b58f43e`). Two of the example's fourteen assertions are about SELECTOR text
# the library does not emit yet — the peer-sibling `:is(:where(.peer):invalid ~ *)`
# rule and the `:nth-child(3)` / `!important` row — and both fail the same way on
# `main.bp:254` and `:264`. Measured from this checkout, which may not edit
# `repository/emilia/**`: nothing in it touches a record, a variant, `is` or a
# `case` arm, so it is not `13-module-identity` half 3's, and the same two
# assertions fail with the half-3 commonJS change reverted. 05-emilia owns the
# selector; this line goes with it.
emilia-modifiers commonJS 05-emilia two selector assertions of front 34's new example (main.bp:254 and :264) name text the library does not emit yet

# `front/36-layout` in `repository/emilia`, landed WHILE this branch was gating
# (emilia `c796ee4` 04:45 and `3d5bf3c` 04:55, merged as `e22cf80`). Both cells
# fail on the front's own new assertions — `display — the eleven § 5.8 values`
# at `emilia.bp:4403` and `regression — no Layout leaf resolves a length` at
# `:4867` — and `emilia-layout` is the example that consumes them. Measured from
# this checkout, which may not edit `repository/emilia/**`: neither assertion
# touches a record, a variant, `is` or a `case` arm, and both were added after
# `scripts/gate.sh --cold` ran green here at 05:05. 05-emilia owns them; these
# MEASURED AGAIN BY THE COORDINATOR, 2026-09-21, AND THE OWNER IS WRONG.
# With the compiler at `a8087490` (C-01 half 3), `modules/emilia` is
# **225 passed / 0 failed on erlang** and **223 / 2 on commonJS** — the same
# source, the same tests, one backend green and the other red. The two failing
# cells are `display — the eleven § 5.8 values` (emilia.bp:4403) and
# `regression — no Layout leaf resolves a length` (:4867), and both read
# `tokenDeclarations(.Layout.<Leaf>)`, which is a `case` over `Token` — the
# construct half 3's step 16 rewrote on commonJS. So this is a commonJS
# divergence introduced by 13-module-identity, not text emilia does not emit
# yet, and the rows below stay only to keep the gate usable while it is
# diagnosed. Do NOT delete them as 05-emilia's: whoever closes the divergence
# deletes them.
# two lines go with the front, and they are expected to be SHORT-LIVED — the
# ledger fails the gate for a listed cell that passes, which is the signal to
# delete them.
emilia commonJS 05-emilia front 36's two new library tests (emilia.bp:4403 and :4867) assert values the dispatcher does not resolve yet
emilia-layout commonJS 05-emilia the example of front 36, red for the same two library assertions
