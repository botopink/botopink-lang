# Library cells `zig build test-libs` knows to be red, each owned by the front
# that turns it green. One cell per line:
#
#   <lib> <target> <owner> <reason…>
#
# A listed cell that fails is reported by name and counted, but does not fail
# the gate. A listed cell that PASSES fails the gate until its line is deleted —
# the owning front deletes it in the commit that fixes the library, which turns
# the cell into a hard assert. An unlisted cell that fails always fails the gate.
#
# Owners are the 1.0.2-beta fronts (specs/1.0.2-beta/fronts.md).

erika     commonJS F1-comptime-dispatch  template body calls primitive methods (split/slice/join) as bare local calls
erika     erlang   F1-comptime-dispatch  template body calls primitive methods (split/slice/join) as bare local calls
jhonstart commonJS F1-comptime-dispatch  html template body calls trim/append/split/slice as bare local calls
onze      commonJS F1-comptime-dispatch  #[mock] decorator body calls push/join/startsWith as bare local calls
rakun     commonJS F12-library-repos     declared dependency 'server' exists under no library root
std       commonJS F3-std-surface        random.bp: `pick expects a type and field names`; path.bp requires the removed gleam_stdlib.mjs
std       erlang   F3-std-surface        random.bp: `pick expects a type and field names`; env/os/process emit invalid Erlang
